version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu
hooks:
  ApplicationStart:
    - location: StartService.sh
      runas: root
  ApplicationStop:
    - location: scripts/stopApp.bash
      runas: root
