version: 0.0
os: linux
files:
  - source: /
    destination: /tmp/
hooks:
  ApplicationStart:
    - location: StartService.sh
      runas: root
  ApplicationStop:
    - location: scripts/stopApp.bash
      runas: root
